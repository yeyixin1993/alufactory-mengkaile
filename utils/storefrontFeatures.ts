// Temporarily disabled at the owner's request. Restore only after explicit
// approval; this controls public presentation, never admin stock or ordering.
export const SHOW_STOREFRONT_INVENTORY = false;
