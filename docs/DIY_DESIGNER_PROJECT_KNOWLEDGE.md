# Mengkaile 3D DIY Designer — Project Knowledge

Last consolidated: 2026-08-12

This document is the durable product and engineering memory for the Mengkaile 3D DIY designer. It consolidates the decisions made during the initial implementation and subsequent user-testing iterations. It is intentionally more specific than a conventional roadmap because many details affect whether customer intent can be manufactured correctly.

## 1. Product goal

Build a browser-based aluminum-profile design and ordering workflow for `mengkaile.top` that combines:

- the visual product library, connection-part presentation, machining workflow, and ordering path seen in JLCFA;
- the smooth camera, movement, rotation, and profile-joining experience associated with MayCAD;
- Mengkaile's existing 2D aluminum-profile configuration, pricing, cart, production sheet, color catalog, pegboard, aluminum board, and marine-board workflows.

The designer is not only a 3D drawing toy. Its primary purpose is to capture unambiguous manufacturing intent, let the customer verify that intent visually, and carry the same intent into the cart, PDF, JSON, and Excel production data.

Do not copy competitor branding, source code, or assets. Competitor products are interaction and visual references only.

## 2. Core product principles

1. **Manufacturing truth over visual approximation.** A pretty model is not sufficient if the A/B/C/D face, groove, end distance, tapping port, screw, or color can be misunderstood by the factory.
2. **Predictable camera and constrained movement.** Left-button dragging the canvas or a part body always orbits the camera, whether or not a part is selected. A part moves only after selection and only by left-dragging one of its six signed X/Y/Z axis arrows; numeric controls remain available for precision.
3. **Helpful snapping without captivity.** Profiles should connect easily, remain snapped while the customer is aiming, and detach without noticeable resistance.
4. **One source of truth.** Stable item IDs, physical groove IDs, dimensions, transforms, machining, tapping, accessories, colors, remarks, and relationships must survive save/load and every export.
5. **Customer and factory must see the same meaning.** 3D presentation may change with camera orientation, but production descriptions use a canonical coordinate convention.
6. **Progressive visual detail.** The assembly remains readable at normal zoom; machining and internal fasteners become clearer through transparency, machining-mark mode, selection, or closer zoom.

## 3. Current entry points and page behavior

- Designer route: `#/diy-designer`.
- The designer should open in a new browser window/tab from its dedicated entry rather than replacing an active shopping workflow.
- The regular homepage banner must retain the original history content. Do not put “3D DIY” back into that banner.
- Desktop and mobile headers retain the original logo, brand text, history, language, login, and cart controls.
- The designer must remain usable on mobile. Do not implement desktop-only assumptions that leave the WebGL canvas blank.
- A special full-screen mode was considered and explicitly deferred. Do not add it unless requested again.

## 4. Designer layout and information architecture

- Left: a compact parts library grouped into profiles, boards, and connections/fasteners.
- The profile library exposes an inline **选择型材型号 / Select profile model** dropdown and a **自由绘制 / Free draw** action. Choosing a model and starting the tool immediately attaches that model's real 40mm section preview to the pointer; it does not open a modal or create a loose/default-length profile first. Customers determine the member length by the existing two-click free-draw interaction, and may switch the dropdown while drawing to continue with another model.
- Center: the 3D workspace with camera controls, direct manipulation, selection, snapping, machining placement, and view controls.
- Right: when nothing is selected, show the project structure/summary. When one item is selected, show its properties and machining. Multi-selection shows batch operations.
- Each scene element has its own remark. The remark is visible in the project structure and must survive JSON, Excel, cart, and production output.
- File actions should be grouped rather than presented as a long toolbar: JSON actions together and Excel actions together.
- “读取本地 JSON / Open local JSON” accepts both designer-export JSON and system order-export JSON (`order_json.items`). Unsupported or empty files must show an explicit visible error notice instead of failing silently.
- “一键填充螺丝 / Auto-fill screws” is a useful assembly action, but should live with connection/machining tools rather than clutter the primary file toolbar.
- “编辑配件 / Edit accessories” detects actual aligned perpendicular profile contacts and initially offers No.1 corner bracket, No.5 slot connector, and drill-and-tap connection editing. The customer first chooses the accessory type, then either clicks an individual highlighted joint or applies that type to every eligible joint.
- Keep the normal canvas view toolbar deliberately minimal: **型材半透明 / Transparent profiles**, **重置视图 / Reset view**, and **显示全部 / Frame all** are the only persistent buttons. Do not show accessory editing, auto-fill screws, machining-mark toggles, or manual snap-alignment choices in this view toolbar. Automatic snapping and manufacturing geometry continue to work without exposing extra controls here; contextual recovery controls such as **恢复鼠标 / Restore cursor** may appear only while their corresponding tool is active.
- The admin dashboard owns profile inventory by **profile model × color × raw-bar length × bar count**. The currently active raw-bar length is fixed at 3.15m; legacy 6m rows remain preserved in storage but are excluded from editing, import/export, and customer-facing totals. Admin-only “坯料 / raw material” stock is available for every model but is also excluded from the public inventory endpoint. The inventory page uses separate model and color dropdown filters and supports direct row editing plus `.xlsx` download/upload.
- Admin analytics count revenue only for confirmed, shipped, or delivered orders. The monthly revenue series uses paid time when available and order creation time as the legacy fallback. The colored-profile Top 5 excludes natural silver and accumulates ordered profile length × quantity in meters from the same eligible orders.

## 5. Selection and direct manipulation

- Clicking any visible part of a profile, board, or accessory selects that exact item. Do not restrict selection to a small hotspot.
- Closely spaced objects must be resolved by the foremost raycast hit. Avoid proxy geometry that causes the neighboring profile to be selected.
- Clicking empty canvas deselects the current item. In drilling mode it also exits drilling and returns to Move.
- Left-button dragging always orbits the camera, even when the drag starts on a selected part. A short left click still selects the foremost part under the pointer; dragging a part body never moves it.
- Right-button dragging the canvas or a part body pans the camera target in screen-space up/down/left/right without changing the assembly or viewing angle; the mouse wheel provides forward/back zoom. A short right click that stays within the click threshold still opens the rotate/delete menu for the foremost part, while a right drag never opens that menu on release.
- Movement requires one selected item. Keep six signed transform arrows: positive and negative X, Y, and Z. Left-dragging an arrow moves only along that arrow's world axis; the part must not drift on either perpendicular axis. Profiles, loose parts, and every accessory—including generated/attached No.1 and other connection hardware—receive the same red/green/blue translation gizmo. Beginning a move on attached hardware releases its original attachment relationship; settling on another valid accessory placement may establish a new relationship. Black two-end length handles belong only to profiles and never appear for accessories.
- Right-dragging the part body moves only the view, never the part. A right click without a drag retains the rotate/delete context menu.
- The former XY/XZ/YZ free-drag work-plane control is removed because free body dragging is no longer part of the interaction model.
- Starting an axis drag opens the numeric movement control for exact distance entry. Numeric movement/length controls must close after Apply or Cancel and must never follow the cursor indefinitely.
- Profiles have black end handles at both ends for lengthening/shortening. Left-drag either handle, show the current length during the gesture, and keep an exact-length editor available as soon as the direction gesture starts. Releasing the handle focuses and selects that value so the customer can type the complete target length directly and press Enter/Apply. Length fields keep empty and partially typed text as an editing draft; they must not coerce an empty draft to the 21mm minimum on each keystroke. Validate only on Enter, Apply, or blur. Values outside 21–3000mm remain editable and show a localized correction message instead of silently clamping or becoming an uneditable `21` prefix.
- Right-click context actions include the three 90-degree rotations and Delete.
- Because customers may not discover the context menu, selecting an unlocked item also exposes the three colored 90-degree rotation controls directly over the scene. Keep a visible scene hint that left-drag on the canvas or part body orbits, left-drag on an axis arrow moves, and right-click offers rotation/delete.
- The persistent scene-control hint uses a high-contrast opaque dark card rather than translucent white text over the grid. Camera controls are separated into three compact chips (left-drag orbit, right-drag pan, wheel zoom), with selected-part movement and right-click actions on a distinct second line. It must remain legible over light profiles, the grid, and translucent assemblies without occupying the central working area.
- `Delete` and `Backspace` delete the selected item(s) when focus is not inside an editable control.
- Shift-click toggles individual items, so a customer can keep Shift held and click every frame member for full-frame batch selection. Starting a Shift-drag on empty canvas opens a blue marquee instead of orbiting; every item whose projected bounds cross that marquee is added to the current selection. Ordinary left-drag without Shift remains camera orbit. Every selected item needs a clear visible highlight so customers can verify batch selection.
- Holding Shift while left-dragging one of a selected profile's colored axis arrows duplicates and moves the copy while preserving the source profile. An ordinary axis drag still moves the selected item without copying, and the explicit properties-panel Duplicate action remains available.
- A selected profile can become the template for a persistent **free draw** tool. Entering the tool shows a short 40mm real-section preview attached to the pointer in the selected profile's current model, color, and finish. The first left click may settle on an existing profile end face, side face, or established profile-to-profile joint; that physical contact is shown as a compact amber patch. Pointer direction then chooses the nearest usable world X/Y/Z axis and shows the live length, while a nearby second contact may magnetically settle the far end. The second left click creates the axis-aligned profile and immediately returns to the short pointer-following preview for the next member. All drawing and intersection confirmations use the left mouse button. Newly drawn members do not inherit drilling, tapping, remarks, or accessory bindings from the template. **Restore cursor / Esc** exits the tool and restores the ordinary left-drag camera-orbit interaction.
- Free draw uses a stable construction plane when the first point is placed in empty space; it must never derive hidden depth from a camera-facing plane. After the first click, pointer projection actively chooses among all usable world X/Y/Z axes. A structural profile end exposes five separate magnetic starts: the end-face centre creates a perpendicular member carried across the top/end face, while four end-edge side starts create a perpendicular member butted against the selected side. The side start follows that side's world-axis normal and shifts the new member back along the target profile by its own half-section so their outside end faces finish flush. Pointer proximity chooses top versus side; neither state may collapse into one centre hotspot. The pointer-following 40mm ghost must use the exact same effective anchor, cross-section offset, orientation, and end-face-flush correction as the committed member, while the amber patch remains on the physical touching face; hovering a top or side start therefore previews the final 90-degree joint without a half-section jump after clicking. Candidate directions that would produce solid interference are deprioritized and cannot be committed. At an existing or just-completed member, perpendicular axes are the only usable directions; a parallel preview must never replace the intended 90-degree member merely because of a temporary collision score. The most recently completed member's far endpoint remains available immediately even before React rebuilds the scene; this lets customers draw a continuous coplanar frame and close the fourth side on exact shared coordinates.
- A single selected profile shows persistent in-scene dimensions derived from current geometry: its own length, useful clearances from an actual connection to the relevant target-profile ends, and the nearest unobstructed world-axis gap to another profile. Labels follow the camera and update while the profile moves.
- The six-axis movement gizmo for a single long profile is view-aware rather than permanently fixed at the profile midpoint. While the customer zooms or pans to inspect either end, anchor the gizmo to the point on the selected profile nearest the current viewport-center ray so at least one usable movement control remains in view. The anchor is UI-only: dragging still moves the whole profile and must not change its manufacturing origin, length datum, or machining coordinates. Freeze the anchor's local offset for the duration of an active drag so the handle cannot jump under the pointer.
- Profile-to-profile contact is highlighted directly on the actual touching region only when the oriented solids are not interfering and their surfaces are within the connection tolerance. Use a restrained amber outline with a very light translucent face, following the MayCAD/JLCFA convention of showing exactly where the two profiles meet. Do not add a detached check-mark badge or redundant “aligned” status icon. This contact patch persists after snapping while the profile remains selected and disappears immediately when contact is broken.
- Accessory editing is a persistent scene mode. Profiles become translucent while every eligible 2020/3030 perpendicular contact is highlighted: purple means clicking adds the chosen accessory, orange means clicking replaces the other connection type, and green means clicking deletes the currently selected type. These joint markers remain clickable while the customer orbits the camera. Separate **一键全添加 / Add all** and **一键全删除 / Delete all** actions operate only on the currently selected accessory type across all eligible joints.
- Provide undo/redo and a **显示全部 / Frame all** view reset that fits every drawn element after the user gets lost while zooming or orbiting.
- Automatic frame fitting is limited to scene membership changes such as adding/removing/importing elements. Changing length, dimensions, position, color, or rotation—including rotating an accessory—must preserve the customer's current camera orbit, zoom, and target; only an explicit **显示全部 / Frame all** action may deliberately replace that inspection view.
- Allow enough zoom-in range to inspect screw and machining geometry.

## 6. Camera and rotation rules

- Camera orbit, pan, and zoom should feel continuous and low-friction, using MayCAD/JLCFA as the primary feel reference: left drag orbits, right drag pans, and the wheel zooms forward/back.
- Rotation is always in 90-degree increments around three colored axes.
- Wording standard: “绕红轴 / green axis / blue axis 90°”. The helper text defines the viewpoint: looking in the arrow direction, rotation is clockwise.
- Repeated rotation must work indefinitely. Four clicks around the same axis equal 360 degrees and must return the object to its original orientation, within floating-point tolerance.
- Rotation around the profile's own longitudinal axis is required so customers can change which slot or covered face is oriented outward.
- Movement, numeric movement, duplicate-and-move, and rotation remain available even when profiles intersect. Do not roll the transform back or show a blocking dialog.
- While interference exists, highlight every involved profile and project-tree node in red and show a non-blocking scene warning. The warning clears automatically after the customer moves or rotates the profiles apart.
- Magnetic snapping should still prefer valid face-to-face joints and skip snap candidates that would create solid penetration; this does not prevent a customer from manually placing an intentionally overlapping intermediate state.

## 7. Snapping and connection intent

Snapping is the highest-risk interaction area and must be tested in all orientations.

### 7.1 Trigger rules

- Profile-to-profile snapping triggers only when the intended contact faces are coplanar or within a small plane tolerance.
- Slot faces connect to slot faces. For multi-slot profiles, target a specific slot centerline rather than the center of the wide face.
- A 2040 wide face can accept two 2020 profiles on its two slot centerlines. Do not force a 2020 into the middle of the 40 mm face.
- For generated wardrobe shelf frames, a 2020 shelf rail meeting a 2040 wide face always uses the groove nearer the usable cabinet interior. The front and rear connections mirror one another geometrically but apply the same inward-groove rule regardless of which physical face is viewed; never place these shelf rails on the 2040 wide-face centerline or the outward groove.
- Prefer end-based structural joints. Profiles normally meet flush without unwanted protruding ends.

### 7.2 Standard snap choices

For ordinary joints, expose three stable alignment candidates along the relevant direction:

- previous/near position;
- same line/center position;
- next/far position.

These correspond to the common module-aligned connection states seen in MayCAD/JLCFA. For 2040-to-2020 joints, normal states are full coverage, no coverage, or a 20 mm module offset. A nonstandard 30 mm overlap is allowed only through explicit numeric positioning; it should not be a preferred magnetic result.

### 7.3 Feel and stability

- Screen-space scaling may keep snapping usable when “显示全部” makes the model small, but it must never create long-range attraction. Joint snap acquisition is capped at 0.72 times the larger cross-section side of the profile being dragged (about 14.4mm for 2020 and 28.8mm for 2040). Axis-only flush alignment is stricter at 0.28 times the larger cross-section and only considers profiles within about two cross-sections on the other axes.
- Snapping has a JLCFA-style magnetic detent: after a joint or flush plane is acquired, the profile remains on that exact plane until the pointer pulls about 0.55 times the larger cross-section away. The consumed pointer travel is treated as resistance; release re-anchors the drag at the snapped position so the profile continues smoothly without a jump.
- While an axis drag is active, flush alignment highlights both corresponding profile faces with thin amber patches. Pointer movement follows a 5mm workshop grid like the JLCFA reference; the compact signed distance reports total travel from the start of the gesture and is anchored beside the gizmo/profile instead of sitting under the pointer. The release editor still accepts 1mm exact values.
- Once a candidate is acquired, keep it stable until the pointer clearly favors another candidate or exits a larger release threshold.
- Separation must be frictionless: snapping helps on approach but adds no extra resistance when the user intentionally drags away.
- Axis movement is continuous and bidirectional even after the customer starts from one signed arrow. While dragging, show a compact pointer-adjacent signed distance to the nearest viable profile connection on that axis: positive on approach, `0.0mm` in the magnetic connection state, and negative after crossing. The highlighted axis handle and this number replace large ghost profiles, detached snap rings, and verbose banners. On release, retain the displaced exact numeric editor for deliberate correction.
- The actively dragged profile stays blue during valid motion and changes immediately to red when it penetrates another profile; a valid face contact uses the restrained amber contact patch defined above.
- Magnetic candidates that would place two solid profiles in the same volume are skipped. Manual movement/rotation may temporarily create interference and uses the non-blocking red warning defined in section 6. Contact at a face or valid structural joint is allowed.

## 8. Aluminum profile geometry

- Profiles must be generated from one coherent cross-section and extruded once. Do not stack several approximate models that look glued together.
- Exterior slots, lips, center bores, cavities, covered faces, and end sections should resemble the real catalog cross-section at both normal view and close zoom.
- Exact catalog/production drawings are authoritative. Existing storefront cross-section diagrams should be reused as references where available.
- Side drilling is subtractive machining, not a decal hidden behind the extrusion. The dark opening, countersunk amber rim, threaded marker, and linked screw must render above the profile body and selected-profile glow, including at grazing camera angles. Countersunk screw geometry remains seated into the removed surface but must not disappear because the opaque profile wins the depth test.
- 2020, 2040, and other multi-slot forms must not be generic rectangular beams.
- 2020R and 3030R have rounded/curved exterior faces and are not ordinary square profiles.
- `2020-N4-RD` is rounded but not a mathematically perfect full circle; model it from its cross-section drawing.
- 2047 is a special section. Its rendered section and machining map must show three end-tapping positions per end.
- Current profile catalog IDs and prices live in `constants.ts`; geometry and machining behavior must remain keyed by stable variant ID, not display name.

## 9. Color and material rules

- The designer uses Mengkaile's existing color pattern. The separate **表面处理 / surface treatment** choice is intentionally omitted for now and may return later.
- `银白 / Silver White` is the light natural aluminum appearance.
- `亮银色 / Bright Silver` is slightly darker with a stronger bright/reflective feel.
- Bright Silver uses the same price tier as other decorative colors. Only Silver White uses the natural-price tier.
- Green ordering and rendering: 冷青绿 is the darkest teal green, 松绿 is the medium green, and 柳绿 is the lightest. 墨青绿 is a separate newly-added green option.
- Current customer-facing color names follow the physical sample labels: 中国红、可乐红、宝石蓝、紫色、浅青蓝、松绿、柳绿、青骊咖、米白、黛蓝、冷青绿、墨青绿、青金、丁香粉、摩卡咖、暗夜黑、深空灰. Legacy stable IDs may remain unchanged in saved data even when their display name changes (for example `black` now displays 暗夜黑 and `british_grey` now displays 深空灰). 橄榄棕 and other colors without a newly supplied sample remain unchanged.
- 可乐红 and 墨青绿 currently support colored cut ends only. They must not offer the natural-cut-end tier until that product availability changes.
- Physical swatch photography is the color/material reference. The individual photos are centered on a consistent 900×1200 canvas with margin and high-detail JPEG encoding; preserve visible matte/gloss/metallic texture ahead of pursuing the smallest possible file size. Only the selected swatch is inserted into the page and it is lazy-loaded, so the full set must not be eagerly preloaded.
- The rotated/cropped all-color overview is shown beside the compact natural-vs-colored cut-end explanation until the customer chooses a specific color. After a specific color is chosen, hide the overview and show only the selected swatch where applicable.
- Aluminum plate and pegboard default to Silver White.
- Pegboard defaults to the IKEA-style hole pattern.
- Marine board defaults to natural wood. The design `wood_natural` value must export/order as `原色 / Original`, including cart and PDF.
- Marine board is a continuous board surface. Do not render horizontal battens/strips that do not exist in the product.
- Connection accessories 1, 2, 5, 7, and 9 default to Silver White.

## 10. Drilling and tapping semantics

### 10.1 Hole placement workflow

1. Customer activates “点选打孔”.
2. Customer first chooses hole type: through, countersunk, or threaded.
3. Customer clicks the intended profile face/slot.
4. A compact numeric editor shows detected entry face, groove, diameter/thread, position, and distances to both profile ends.
5. Customer confirms; clicking outside cancels drilling and returns to Move.

### 10.2 Physical hole model

- A through hole passes through the entire profile. If entered on A, the exit is visible on C; B exits D.
- A countersunk hole has a larger recess on the entry face only. The opposite face remains a normal through-hole exit.
- Hole overlays must remain legible from every camera angle. Countersunk circles must not appear heavily clipped because a decal intersects the surface.
- A through-hole visual diameter must not exceed the usable slot width.
- Threaded holes use a dashed/thread cue so they cannot be mistaken for through holes.
- Machining indicators should be readable without covering one another; use selected-item emphasis, leader/offset marks, transparency, and machining-mark mode instead of piling every label at the same depth.

### 10.3 Canonical faces and grooves

- A/B/C/D are canonical manufacturing faces, not camera-relative labels.
- Store `entryFace` plus a physical groove identifier/index. Derive the displayed groove ordinal for the currently described face.
- Opposite mappings: A ↔ C and B ↔ D.
- For a two-groove wide side, displayed groove order reverses on the opposite face. Example: B面第一槽 = physical P1 = D面第二槽.
- The production PDF/Excel must show both entry and exit paths so customer and factory can verify the same hole.

### 10.4 End tapping

- Show end tapping directly on the profile end face using red indicators, not yellow.
- Support no tapping, left end only, right end only, and both ends.
- Multi-port sections expose each port independently. 2040-class multi-bore profiles can have multiple ports; 2047 has three ports per end.
- Port count and location come from the real cross-section, never from a generic single-center assumption.

## 11. Screws and connection hardware

- Auto-fill mapping is a Mengkaile product rule:
  - countersunk hole → cylinder-head socket screw;
  - through hole → button-head socket screw;
  - threaded hole → no automatic screw.
- Automatically filled screws default to Silver White/natural, not black or another expensive decorative color.
- Screw heads should not protrude outside the profile envelope. The shaft passes through the first member and extends into the matching tapping channel/port of the second member to communicate the connection intent.
- Transparency mode should make internal screw paths readable. Normal mode should avoid excessive visual clutter.
- Individual socket-head screws and leveling feet are currently hidden from the parts library. Existing saved designs containing them remain load-compatible.
- The legacy cart checkbox “需要配304螺丝” remains the source for ordinary non-designer orders. A designer order with explicit screw items uses the customer-confirmed designer quantities instead; explicit designer screws take precedence and must not also incur the legacy per-hole screw fee.
- Factory/customer PDFs always retain the prominent “螺丝配件提醒：需要配螺丝” whenever either source requests screws. Preserve the legacy per-profile quantity wording for ordinary orders; designer orders display their confirmed quantities in the same summary area.
- Designer screw rows are compacted by source profile model/compatible series, head type, length, color, and unit price. The PDF shows one compact screw-summary section with a few specification rows, not one full accessory card per physical screw. Customer quantity edits in the designer are authoritative and survive cart/PDF/production output.
- For the 20-series system, the purchasing/PDF specifications are fixed catalog SKUs: cylinder-head socket = M6×30, button-head socket = M6×20, and the wardrobe flat-head socket override = M6×8. Other profile series resolve through the same catalog specification table.
- For the 30-series system, cylinder-head socket = M8×45 and button-head socket = M8×20 plus the elastic/spring fastener. The spring fastener is part of the button-head kit and must remain visible in the designer specification and PDF/BOM wording.
- Internal 3D screw geometry uses the same catalog length as cart/PDF output. Ray-fitted connection depth may still determine placement and penetration direction, but it must not change the modeled or ordered shaft length.
- One-click drill-and-tap creates one editable countersunk passage through the crossing member, applies tapping to the contacted end of the perpendicular member, and creates the linked catalog-length cylinder-head screw. The hole carries a stable generated-joint key so repeated generation is idempotent and JSON/XLSX can preserve the relationship.

## 12. Connection accessory catalog

Availability and price must be verified against Mengkaile's authoritative catalog before production release. The current implemented matrix is:

| Part | Meaning | Available series | Geometry rules |
| --- | --- | --- | --- |
| No.1 | Corner bracket | 2020, 3030 | Full-width die-cast corner seat with perpendicular base/upright faces and one face-normal socket fastener on each face. 2020 seating lengths are 20/28mm and depth is 20mm; 3030 is 30/30/30mm. Must remain visually distinct from the fixed-50mm-leg extruded No.2. |
| No.2 | Extruded corner bracket | 1515, 2020, 3030 | Extruded-bracket form; do not reuse No.1 geometry. |
| No.5 | Hidden bracket | 2020, 3030 | Hidden connector with one fastening hole on each mounting face. |
| No.7 L | L connecting plate | 1515, 2020, 3030, 4040 | 2 mm thick. 1515 and 2020 share one physical size/model. |
| No.7 T | T connecting plate | 1515, 2020, 3030, 4040 | 2 mm thick. 1515 and 2020 share one physical size/model. |
| No.9 | Three-way end connector | 1515, 2020, 3030 | Exact series cube: 15×15×15, 20×20×20, or 30×30×30mm. Three mutually perpendicular profile ends seat flush on three cube faces; the opposite three exposed faces carry the round installation openings. No decorative protruding arms. |

- These accessories default to Silver White.
- One-click No.1/No.5 generation currently supports aligned, touching, same-series 2020/3030-system perpendicular joints. At a joint, both perpendicular seating edges of No.1 must lie flush on their respective profiles and the generated part locks to both profile IDs. In persistent library placement, No.1 also exposes every physically open groove face of a single profile (four faces on an ordinary four-slot section), matching MayCAD `SlotAccessory` semantics; this single-profile placement must not imply a completed two-profile joint. No.5 uses the same joint axes but sits at the groove plane. Repeating one-click generation must not duplicate an existing part at that joint.
- In accessory-edit mode, choosing a different supported type at an occupied joint replaces the generated No.1/No.5 or drill-and-tap relationship at that joint instead of stacking incompatible connection methods. Clicking an installed green joint or using Delete all removes only the currently selected type. Removing a generated drill-and-tap passage also removes its linked auto-filled screw and generated end-tap state; do not remove unrelated customer-created machining.
- The other-hardware library exposes a threaded-stem twin-wheel caster, an adjustable cabinet foot, and aluminum profile end caps. The caster supports M6/M8/M10/M12 stem selections and brake/no-brake variants, but its body color is fixed black and the editor must not offer a color selector. Its supplied reference does not confirm wheel/body dimensions, so its 3D body is a proportional customer-selection preview rather than a manufacturing-dimension authority.
- The current leveling-foot preview follows the supplied MayCAD `LVL_FOOT_1.44.002403` cup-foot form: a threaded M8 stem, lock nut, shallow load-spreading metal cup, and dark nonslip sole. It replaces the earlier unrelated 70×70mm cabinet-leg placeholder. Dimensions and pricing remain provisional until Mengkaile confirms the production catalog; it has no decorative color selector.
- An end cap is never a free-floating accessory: the customer first selects a profile and then adds the cap to the first available end (left before right). It follows the profile transform and exact profile model. If the chosen end was not tapped, adding the cap automatically adds that end's tapping and records ownership; deleting the cap removes only tapping that cap added, never pre-existing/manual tapping. End caps support every profile color.
- End-cap 3D geometry follows the MayCAD `CAP20SQ2020` construction language: a face plate matching the selected profile section plus a smaller rear locating plug entering the profile end. The visible countersunk fastening pattern remains driven by the selected Mengkaile profile tapping grid.
- No.7 L/T plates are valid profile-to-profile hardware only when their geometry candidate binds both mating profile IDs. Their fasteners must visibly land on both arms/sides; deletion or loss of either target invalidates the locked installation instead of leaving a one-sided floating plate.
- Caster and adjustable-foot prices remain provisional estimates and must be labelled accordingly: caster ¥18 base, brake +¥4, M10 +¥2, M12 +¥4; adjustable foot ¥12. End-cap catalog pricing is confirmed for 2020 at ¥3 retail / ¥2.7 bulk and 3030 at ¥4 retail / ¥3.5 bulk. The same end-cap price applies to every color, and the existing bulk threshold is 20 pieces of the same catalog line. Other end-cap sizes remain provisional until confirmed. Client-side totals remain non-authoritative.
- Bright Silver is priced as a decorative color, the same tier as other colors.
- Compatibility filtering must hide impossible size/part combinations rather than letting the customer create them.
- Designer SVGs represent simplified 3D/vector geometry only.
- Physical-product JPGs under `public/images/accessory/` must never be overwritten by SVGs. JPGs are the authoritative visual references for the storefront, cart, customer PDF, and factory PDF.

## 13. Save, load, export, cart, and production

### 13.1 Local design files

- Initial persistence is file-based and local; do not silently depend on cloud storage or browser cache.
- **Save design JSON** downloads an editable source document.
- **Open local JSON** restores the complete editable assembly.
- The JSON source must preserve stable IDs, item kind/model, dimensions, color/material, transform, quantity, remark, holes, physical groove mapping, tapping ports, accessories, screw-hole links, and future joint relationships.

### 13.2 Production files

- **Production JSON** is a factory-oriented structured export.
- **Production Excel** must be a real `.xlsx` ZIP workbook, never SpreadsheetML/XML renamed to `.xlsx`.
- **Open local Excel** reads a designer-generated workbook and reconstructs the item list and estimate. Unsupported workbooks should fail clearly rather than partially guessing.
- Every profile row includes model, length, color, quantity, transform/assembly identity as needed, remark, hole details, tapping details, and pricing inputs.
- Machining sheets use canonical A/B/C/D faces, physical groove IDs, entry/exit descriptions, and both end distances.
- Cart conversion and the production PDF must preserve marine-board “原色”, accessory specifications, physical-product image keys, and all customer remarks.
- Cart/PDF conversion must preserve designer screw length, head type, compatible profile series, source profile model, color, customer-confirmed quantity, price, and linked profile/hole references while grouping identical screw specifications.
- Customer/factory PDF remarks contain only customer-entered or manufacturing-relevant notes. Scene position, rotation, “3D design”, and other editable-model transport metadata stay in JSON/XLSX and must not be appended to the visible remark column.
- A production-sheet preview with no selected shipping address or a displayed shipping fee of zero is labelled freight collect/到付/着払い. It must not imply ordinary prepaid courier service before the complete address, shipping, PDF, and payment flow has run.
- Shipping-weight baseline for marine board must use area multipliers: 12mm = 8 kg/㎡ and 18mm = 12 kg/㎡. If any legacy value differs, this baseline is authoritative.
- Client-side prices are estimates. A production deployment needs server-side validation against the current catalog before checkout totals become authoritative.

### 13.3 MayCAD interoperability

- MayCAD `.scene` files are XML and contain authoritative per-profile transform matrices and machining locations. Import them deterministically in the browser; do not spend AI tokens when the source scene is available.
- MayCAD profiles extrude along local +Y, while Mengkaile profiles use local +X. Convert the complete basis and center point, preserve one editable item per physical member, and keep source IDs in remarks for review.
- Verified 20-series mappings include `2020/43SP` → `2020`, rounded `21SP` → `2020R`, adjacent-face `22SP` → `2020-N2`, opposite-face `23LP` → `2020-N2-OPP`, one-face `33SP` → `2020-N1`, and all approved `2040/44SP/64SP` variants → ordinary `2040`. MayCAD does not provide a Mengkaile-equivalent covered-face 2040 in this verified set. For 30×30, Soft/Q variants map to `3030R`, two-face 3002 and `.22` variants to `3030-N2`, 3H/3003 variants to `3030-N1`, and ordinary 4H 3030L/SP variants to `3030`; Mengkaile currently has no separate opposite-face 3030 SKU. Approved 3060, 4040, and 4080 variants normalize to their ordinary Mengkaile models. Keep `docs/MAYCAD_PROFILE_MAPPING.md`, the importer, and the PDF AI prompt synchronized.
- A MayCAD assembly PDF contains BOM, machining tables, orthographic views, and isometric/exploded images, but no embedded editable scene coordinates. PDF/image AI reconstruction is deferred and disabled in the current product; normal local and production deployments require no AI provider or API key.
- There is no active customer PDF entry point, frontend API method, or registered backend AI route. Experimental reconstruction code may remain for future evaluation only. If enabled in a later release, every inferred import must be editable and carry confidence and review warnings; never silently claim manufacturing certainty for inferred face orientation, hidden members, machining, tapping, color, boards, or accessories.
- Imported MayCAD blind bores are not equivalent to Mengkaile end tapping. Unsupported blind bores remain review warnings rather than being converted into fabricated machining intent.
- Exact verified MayCAD profile codes may import directly. Every unverified or unknown `.scene` profile must open a post-import model confirmation step, grouped by source code and cross-section where possible, with the dimension-based fallback shown only as a suggestion. Model confirmation precedes the tapping prompt.
- The current MayCAD import scope is profiles plus recognized drilling records only. Accessories, fasteners, and connectors are not imported yet; the UI must state that accessory import is under development in both the import review and confirmation flow.
- The customer-facing MayCAD import picker accepts `.scene` files only. Filter the picker to `.scene` by default and reject any PDF, image, renamed document, or other extension before parsing with a localized instruction to choose a MayCAD-exported `.scene` file. Experimental PDF reconstruction is not exposed, and its backend blueprint is not registered unless a future deployment explicitly sets `ENABLE_MAYCAD_AI_IMPORT=1` after provider readiness.

### 13.4 External AI JSON handoff

- External aluminum-profile AI systems may hand designs to Mengkaile through the editable `mengkaile-diy` JSON contract (`schemaVersion: 2`). The maintained copy-and-paste prompt and field rules live in `docs/AI_PROFILE_ORDER_TO_MENGKAILE_JSON_PROMPT.md`.
- Geometry remains authoritative: every physical scene member has its own item, millimetre dimensions, centre position, and degree-based rotation. A quantity-only BOM is not enough to reconstruct an assembly and must trigger clarification instead of guessed coordinates.
- Imported third-party JSON must never make an external quoted total authoritative. Mengkaile recalculates the estimate from current catalog IDs, lengths, colors, machining, membership rules, and installed accessories so recoloring and checkout remain consistent.
- External AI must not invent a near-match profile, machining face/groove, or accessory location. Unsupported or incomplete source data requires clarification before a ready-to-order JSON is produced.

## 14. STEP/STP interoperability direction

The requested long-term workflow is similar to a profile-quote page:

- upload STEP/STP;
- identify/list parts for customer verification;
- map recognized profiles and machining to Mengkaile catalog items;
- create a cart directly or import the recognized assembly into the designer for editing.

Recommended architecture:

1. Keep editable designer JSON as the native source of truth.
2. Implement server-side STEP parsing and geometry recognition; browser-only parsing is not sufficient for reliable manufacturing interpretation.
3. Require a customer verification table for model, length, color, machining, quantity, and recognition confidence before cart conversion.
4. Separate “assembly STEP for external CAD” from “manufacturing STEP/cut parts”.
5. Do not promise round-trip STEP fidelity until exact profile B-reps/CSG, boolean drilling, coordinate conventions, units, and accessory models are finalized.

STEP import/export is a roadmap item, not a currently guaranteed feature.

## 15. Visual reference lessons

### From MayCAD

- Camera orbit/pan/zoom and profile dragging should feel smooth and predictable.
- Profiles should be easy to bring onto the same working plane.
- Standard modular connection placements should be offered while dragging.
- Rotation controls must make longitudinal self-rotation obvious.

### From JLCFA

- Catalog cards, cross-section recognition, connection hardware, transparent profiles, and assembly-level BOM presentation are useful reference patterns.
- Machining and connection components should be visible in context but organized so dense joints remain understandable.
- The project structure is an important customer verification surface, not merely an internal scene graph.

## 16. Implementation map

- `components/DIYDesigner.tsx`: scene state, Three.js rendering, selection, transform gestures, snapping, drilling, tapping, accessories, pricing estimate, file actions, and cart conversion.
- `utils/profileMachining.ts`: canonical face/opposite-face rules and display-groove ↔ physical-groove conversion.
- `utils/productionXlsx.ts`: real XLSX creation and workbook import.
- `components/ProfileVisualizer.tsx`: storefront/production profile machining visualization.
- `components/FactorySheet.tsx`: customer/factory summaries, machining details, colors, accessory image mapping, and PDF-facing content.
- `constants.ts`: profile variants, colors, prices, weights, localized labels, and marine-board natural-color mapping.
- `public/images/accessory/*.svg`: designer-only accessory artwork/geometry references.
- `public/images/accessory/*.jpg`: physical-product references for storefront/cart/PDF/factory output; preserve them.

## 17. Regression acceptance checklist

Before releasing a designer change, verify at minimum:

### Scene and input

- Empty designer renders on desktop and mobile.
- Add each of: one profile, aluminum plate, IKEA pegboard, marine board, and every visible accessory.
- Select crowded adjacent profiles correctly and deselect by clicking empty canvas.
- Left-drag on empty canvas, an unselected part, and a selected part always orbits and never changes any part position.
- A part body cannot be freely dragged with either mouse button.
- A selected unlocked part shows all six ±X/±Y/±Z arrows. Hover each arrow and verify it highlights, then left-drag it and verify movement stays on that signed world axis, with no perpendicular drift or crossing into the opposite direction.
- Each axis arrow opens the correct numeric movement UI immediately and closes correctly.
- Hover both black end handles and verify they highlight; left-drag either handle to resize from that end, and verify exact length entry works.
- Shift-click and Shift-marquee show all selected items visibly. Shift-dragging a selected profile's colored axis creates a displaced copy while the explicit Duplicate button still works.
- Selecting a profile shows its length plus relevant connection clearances/nearest axis gaps. Orbit the camera and move the profile; labels remain attached to the correct geometry and update numerically.
- Every true profile contact shows a restrained amber outline/light face exactly on the touching region, with no detached check-mark badge. Pull the profile away and verify it disappears; create solid interference and verify the contact is not falsely marked connected.
- Delete/right-click delete, duplicate, undo, redo, and Frame all work.

### Rotation and snapping

- Rotate four times around each colored axis and compare with the starting transform.
- Self-rotate a profile and verify the visible slot/covered face changes.
- Verify the in-scene rotation toolbar appears for a selected unlocked item and that the persistent right-click hint is visible.
- Test snapping for parallel, perpendicular, vertical, horizontal, and camera-oblique arrangements.
- Test 2020↔2020, 2020↔2040 on both wide-face slots, and multi-slot↔multi-slot.
- Verify snap acquisition at Frame-all zoom, stable hold near a candidate, easy separation, and no solid penetration in the proposed snap.
- Intentionally move and rotate profiles into interference: the transform must remain applied, every involved profile/tree node must turn red, and the warning must clear after separation.

### Machining

- Place through, countersunk, and threaded holes on A/B/C/D.
- Verify entry and opposite exit are visible and the countersink is larger only on entry.
- Verify two-slot B1 maps to D2 physically and exports consistently.
- Verify end tapping options and every port on 2040/multi-port variants and 2047.
- Toggle transparency and machining marks; inspect a crowded joint from multiple angles.
- Auto-fill screws and verify type, natural color, embedded head, linked hole, and penetration direction.
- Run each one-click connection mode on touching 2020 and 3030 perpendicular joints. Verify No.1 sits outside the solid faces, No.5 follows the groove plane, drill-and-tap adds the passage/tapped end/linked screw, and repeating the same mode does not duplicate the joint.
- Verify 2020 screw kits show/model M6×30 cylinder-head and M6×20 button-head; verify 3030 shows/models M8×45 cylinder-head and M8×20 button-head plus elastic fastener in the designer and PDF/BOM.
- Change auto-filled screw quantities manually, add the design to cart, and verify the PDF reminder plus compact grouped rows use the edited quantities without adding the legacy per-hole fee or repeating one card per screw.

### Persistence and commerce

- JSON save → load preserves the full editable assembly.
- XLSX export is recognized as a real workbook; XLSX import reconstructs parts and estimate.
- Cart and factory/PDF output preserve remarks, colors, marine “原色”, machining, tapping, accessory specifications, and JPG image mapping.
- `npm run build` passes. Treat bundle-size warnings as performance work, not a functional failure.

## 18. Known risks and next priorities

1. Verify every profile cross-section against production CAD/section drawings, especially 2020R, 3030R, 2020-N4-RD, and 2047.
2. Expand automated tests for screen-space snapping, hysteresis, collision, 360-degree rotation, and dense-scene selection.
3. Introduce explicit joint entities so two profiles, their holes/taps, connector, and screws form one editable relationship rather than unrelated scene items.
4. Improve dense-joint annotation layout and transparency behavior without hiding manufacturing details.
5. Move accessory availability/pricing into a single authoritative catalog shared with the storefront and server.
6. Add server-side price validation and eventually account/cloud design storage while retaining local JSON portability.
7. Generate canonical customer/factory 2D A/B/C/D drawings from the same physical machining model.
8. Design and validate the STEP/STP quote/import pipeline before adding STEP export.
9. Reduce the large Three.js designer bundle through deliberate code splitting after behavior stabilizes.
10. Build an AI-native design command layer on the same editable scene schema: natural-language prompts produce constrained scene operations, the designer renders the result, and a human reviews it before cart/order conversion.

## 19. Local development and version safety

- Install: `npm install`.
- Run: `npm run dev -- --port 3000`.
- Designer: `http://127.0.0.1:3000/#/diy-designer`.
- Build: `npm run build`.
- Avoid leaving servers on 3001/3002/3003; keep the active local experience on port 3000.
- The initial designer feature was merged into `main` at commit `aaad4fe448de5c1b3af9fbc57047f4a06cf1685f`.
- Pre-merge main is preserved as `backup/main-before-3d-designer-20260801` at `c578814be24ea97ed975df51c9c045116835e92e`.
- `feature/3d-diy-designer` remains as a historical feature branch.

## 20. Maintaining this knowledge

- Update this document in the same change whenever a product rule, machining convention, compatibility matrix, or export contract changes.
- Add a dated note below for decisions that reverse an earlier rule.
- Do not record passwords, tokens, customer personal data, or temporary local-machine state here.
- When implementation and this document diverge, create an explicit issue/backlog entry and state which behavior is currently shipping.

### Decision log

- **2026-08-20:** Added 2020 and 3030 aluminum-profile end caps to the customer accessory quote catalog and confirmed their pricing. 2020 is ¥3 retail / ¥2.7 bulk; 3030 is ¥4 retail / ¥3.5 bulk; the same rate applies to natural and every decorative color. Bulk pricing follows the existing same-line threshold of 20 pieces. The designer now reads these confirmed prices for matching 2020/3030 end caps instead of the former ¥6 natural / ¥8 colored placeholder, while unconfirmed end-cap sizes retain their clearly labelled provisional fallback.

- **2026-08-20:** Fixed exact profile-length editing at both entry points. The properties-panel length field now keeps a temporary text draft while the customer deletes/retypes the number and commits only on Enter or blur. Empty, nonnumeric, below-21mm, and above-3000mm drafts are not written into the scene; they remain visible with a localized correction notice until fixed. Starting either black-end resize gesture now opens the floating current-length editor immediately, continues updating it during mouse drag, and focuses/selects the final length on release so a large-frame member can be set by direct keyboard entry without a second selection workflow. Enter/Apply uses the same 21–3000mm validation and preserves the chosen fixed end when recentering the resized profile.

- **2026-08-20:** MayCAD import now synchronizes the accessory-series selector to the dominant imported profile series. Accessory insertion also falls back to the dominant compatible series already present in the scene when the currently selected series has no profiles, and updates the selector to show the series actually used. This prevents a validated all-3030 import such as `SHOWCASE.scene` from falsely reporting zero No.1 placement candidates merely because the library retained its default 2020 filter. Mixed-series scenes keep the explicitly selected series whenever that series is present.

- **2026-08-13:** Rebuilt No.2 from the supplied press-extruded `5020` reference. Its two perpendicular seating legs are both fixed at 50mm for every supported family; the extrusion depth alone follows the matched profile width, producing 50×50×15mm for 1515, 50×50×20mm for 2020, and 50×50×30mm for 3030. The 3D body uses two full-depth seating rails, one triangular reinforcing web, and one socket-head fastening position on each leg. Installation candidates require two different, mutually perpendicular, physically contacting profiles with at least 50mm of unobstructed rail in both directions. The bracket origin is the true joint corner, both seating faces remain outside and flush to their profiles, and any candidate whose full-depth cast arms would enter either extrusion is rejected.
- **2026-08-13:** Corrected No.1 from the provisional MayCAD thin-gusset interpretation to the supplied physical die-cast corner-seat reference. No.1 now fills the matched profile width and renders a continuous base, continuous upright, two side reinforcing ribs, and two socket-head fasteners whose axes are perpendicular to the two profile faces. The 2020 part is 20mm along the base, 28mm up the upright, and 20mm wide; the 3030 part is 30×30×30mm. Placement probes the true shared inside-corner edge of two physically touching perpendicular profiles, keeps both cast seating faces flush, rejects penetration into either target or a third blocking profile, and records both profile IDs. This supersedes all earlier No.1 thin-plate geometry and exterior coplanar-face placement descriptions.
- **2026-08-13:** Rebuilt No.5 from the supplied single-hole slot-connector reference. It is now a shallow cast L with one flush hex-socket position per arm, not an exterior corner block: both narrow arms extend inward from a real perpendicular profile contact and sit below one shared coplanar slot face, leaving only the metal strip and two sockets visible through the grooves. The two opposite usable slot planes produce correctly flipped installation candidates; non-coplanar faces are rejected. Add-all, individual placement, attachment syncing, and delete-all use the same two-profile joint key, including the face suffix, so installed No.5 parts remain bound to both profiles and can be removed reliably.
- **2026-08-13:** Added a MayCAD-import normalization pass for shallow perpendicular endpoint interference, including 3030 assemblies authored with 20-series centerline clearance. Imported geometry now uses the mapped profile's real section on every axis: when a rail end enters a perpendicular member by no more than that member's projected half-section, the rail is shortened and recentered until its end face touches the target's outer face. Ambiguous L-corners preserve long vertical posts and trim the horizontal/depth rail. The pass repeats so both ends of an internal shelf rail can be corrected independently. If the repaired end changes the local start datum, imported hole positions are translated by the same amount; any repair that would cut into an existing hole is rejected for manual review. The pass does not suppress parallel overlap, mid-member crossings, or deep penetration; those remain genuine red interference requiring review. This is an import-boundary repair only and does not change native designer geometry or the source-to-SKU mapping. The supplied showcase validation below supersedes the earlier assumption that its endpoint placement needed this repair.
- **2026-08-13:** Validated MayCAD import against the supplied real `SHOWCASE.scene` (46 × `PROF30-3030L`). MayCAD uses centimetre transforms that legitimately produce half-millimetre profile centres: its 855mm rails sit between 30mm posts on coordinates such as -437.5mm. Import now preserves 0.1mm position precision instead of rounding centres to whole millimetres. This keeps the original 855mm/2000mm/340mm cut lengths intact, makes the intended rail ends exactly touch the post faces, and removes the false interference that whole-millimetre rounding introduced. The shallow-intersection repair remains only for files with genuine legacy endpoint penetration; it must not alter this validated showcase assembly. True deep/parallel overlap continues to raise the red interference warning.
- **2026-08-13:** Corrected free-draw closure interference. Once a snapped start is displaced by half a cross-section to form an end-to-side butt joint, snapped length is measured from that effective displaced anchor rather than the original profile centerline. When the other end targets a perpendicular profile endpoint, the target's projected half-section is also removed from the run, so the new end face stops at the target side face instead of entering its solid (10mm for ordinary 2020). Preview collision rejection and the final data-boundary collision check remain active as independent safeguards.
- **2026-08-13:** Rebuilt the accessory geometry and placement semantics from the supplied MayCAD `parts.scene`. The initial thin No.1 interpretation in this entry is superseded by the later physical-reference correction below. Accessory and end-cap holes use transparent-center metal rings instead of opaque black discs. No.7 L/T plates visibly fasten both arms and remain valid only while both target profiles survive. End caps now include the `CAP20SQ2020`-style rear locating plug in addition to the profile-matched face/tapping pattern. The foot preview is superseded by the compact `LVL_FOOT_1.44.002403` M8 stem, lock nut, cup, and nonslip sole rather than the unrelated 70×70 cabinet-leg placeholder.
- **2026-08-13:** Replaced the profile-library add modal with direct model selection and free draw. The profiles section now keeps the complete catalog in an inline dropdown; clicking Free draw immediately enters the pointer-following two-click drawing tool using that selected section. Changing the dropdown while the tool is active swaps the preview/template for the next member without creating any item, and Restore cursor/Esc exits normally. The old 200mm loose-profile creation modal and the duplicate canvas free-draw entry are removed.
- **2026-08-14:** Restored the reference designer's two Shift gestures after reviewing the supplied recording. Shift-dragging a selected profile's colored movement axis now preserves the source and commits a copy at the dragged position; Shift-dragging from empty canvas draws a visible blue marquee and adds every intersecting projected part to the existing multi-selection. Shift-click remains the precise per-item toggle, while ordinary left-drag continues to orbit.
- **2026-08-12:** Compacted the post-drag exact movement editor because customer movement values are normally at most three digits and the former 230px two-row card obscured the selected profile. The released editor now uses a single 184px row with a three-digit-width input and parks on the viewport edge opposite the released axis handle; the already-compact live signed-distance label remains near the gizmo during the drag.
- **2026-08-13:** Corrected free-draw frame construction after comparison with the supplied JLCFA workflow. Empty-space starts now use a stable construction plane instead of a camera-facing depth plane; pointer movement switches live among world X/Y/Z; side-face starts seat outside the target profile; endpoint centers and the just-created member's far end receive exact magnetic continuation targets; and overlapping candidates cannot be committed. Continuous four-member frames therefore remain coplanar and can close on shared coordinates. Shift-click continues toggling every member for full-frame selection. Also narrowed automatic camera fitting to scene membership changes, so rotating a profile or accessory no longer kicks the customer back to an overview.
- **2026-08-12:** Added a persistent JLCFA-style library placement mode for No.1 corner brackets, No.7 L/T connector plates, and No.9 three-way end connectors. Selecting one of these parts no longer drops a loose object at the scene origin: every geometry-compatible same-series installation candidate appears immediately as a purple hotspot, hovering a hotspot previews the correctly oriented part, and left-clicking installs/replaces it while leaving the tool active for repeated work. Clicking a non-candidate profile surface reports “当前位置无法安装此配件” at the pointer; Esc or the restore-pointer button exits. No.1 exposes both installable profile-face orientations. No.7 uses the current provisional model dimensions for minimum-clearance checks (L arms must fit from the corner; T crossbar/stem must fit their respective runs), pending confirmed production dimensions. No.9 remains restricted to three mutually perpendicular converging profile ends and now shortens/moves legacy common-endpoint members by the required half-module face clearance so the connector cube occupies the node without overlapping the profiles.
- **2026-08-12:** Replaced the prominent floating yellow connection check mark with a MayCAD/JLCFA-style contact patch. Valid profile connections now show only a thin amber outline and subtle translucent fill wrapped tightly around the actual touching area, so the customer can see which two surfaces meet without interpreting a status symbol. The patch persists only while the selected profile remains physically connected and disappears on separation or interference.
- **2026-08-12:** Stabilized 3D click selection for thin profiles and end caps. A simple left click no longer retargets the orbit pivot on pointer-down, and selection raycasts use the original pointer-down coordinate so tiny camera motion cannot make a part escape before pointer-up. When an already-selected profile's transform/length hit area overlaps a different visible part, the visible part takes selection priority; deliberate handle dragging remains available where no other part is underneath. Adding an aluminum end cap to a selected profile now fills every missing end in one action (normally both ends), preserves an existing cap, independently records/removes only the auto-owned tapping for each end, and keeps left/right caps as separate project-summary rows so their installation ends remain explicit.
- **2026-08-12:** Changed selected-part manipulation from right-drag to left-drag on explicit handles. Left-dragging empty canvas or a part body still orbits the camera, while left-dragging a red/green/blue signed axis arrow moves only along that direction and left-dragging either black profile-end arrow resizes from that end. Hovered handles highlight before dragging. Right-click is now reserved for the part action menu, eliminating the competing movement/menu gestures. The floating move/length input is displaced substantially away from the picked handle, and the selected-profile length remains as a remote label without drawing the former full-span blue dimension line behind it.
- **2026-08-12:** Temporarily disabled new half-overlay cabinet-door selection pending correct edge/corner hinge rules; legacy half-overlay data remains readable and can be switched to another mode. Full overlay continues sizing to the outside frame bounds and sits in front so it hides the frame, while large-bend/inset sizes to the inner clear opening and sits behind the front face so all four frame profiles remain visible. The 2mm margin applies to the selected full-overlay outer bounds or inset clear-opening bounds respectively.
- **2026-08-12:** Rebuilt the generated demo workbench as an assembled onboarding example rather than loose visual geometry. The four adjustable feet meet the four leg bottoms; all legs terminate against the underside of the top profile frame; two rear uprights contact the rear frame and physically carry the pegboard; the 18mm marine-board worktop rests flush on the complete top frame. Every valid profile contact receives a locked No.1 bracket with stable joint/profile references. The worktop receives four locked No.7 mounting plates, and the pegboard receives four locked No.7 mounting plates tied to the exact panel and support-profile IDs. Removing an attachment target removes its panel connector instead of leaving loose hardware.
- **2026-08-12:** Added a dedicated reset-view command separate from frame-all. Reset view always recenters the complete assembly, fits it within the current viewport, restores the world-up axis, and returns the camera to the fixed upper-front-right default isometric direction. Frame-all continues to preserve the customer's current viewing direction while only refitting the assembly.
- **2026-08-12:** Added a persistent desktop divider control between the 3D canvas and the right project/properties panel. Collapsing the panel reallocates its full width to the design canvas; expanding restores the original three-column layout. The 3D viewport must resize with the layout rather than stretching or retaining stale blank space. Mobile keeps the normal stacked project panel so the control does not hide required editing content on narrow screens.
- **2026-08-12:** Enabled auto-fit cabinet doors for complete axis-aligned front profile frames, including generated wardrobe frames. The door tool remains disabled until the designer detects two front horizontal rails and two front vertical posts. A door is bound to those four profiles, stays auto-sized when the frame changes, and uses a 2mm perimeter margin. Customer-selectable materials are aluminum cabinet door, marine-board door, and aluminum pegboard door; their material rates reuse the existing aluminum-frame-door/2mm aluminum plate, marine-board, and 2mm pegboard price rules, respectively, with the normal door hinge fee. Full overlay covers the outer frame, half overlay reaches the midpoint of each profile face, and large-bend/inset fits inside the clear opening. Left/right opening is represented by matching hinge and handle sides in 3D. A single leaf remains limited to 1500×3000mm. JSON/XLSX/cart/factory data preserve material, overlay, opening side, hinge positions, 2mm margin, and attached frame IDs.
- **2026-08-12:** Enabled the first JLCFA-inspired one-click connection workflow for actual aligned perpendicular profile contacts: No.1 corner bracket, No.5 groove/slot connector, and drill-and-tap. Generated No.1/No.5 parts are geometry-positioned on the joint, locked to both profile IDs, and deduplicated by their attachment key. Drill-and-tap writes an editable countersunk passage, the matching contacted-end tap, a linked catalog screw, and a stable joint key preserved by JSON/XLSX. Confirmed catalog screw systems are now 2020 cylinder M6×30 / button M6×20 and 3030 cylinder M8×45 / button M8×20 plus elastic fastener. This supersedes the deferred fitted-length behavior: internal 3D screw shafts now use the same catalog length as the designer/cart/PDF specification.
- **2026-08-11:** Removed Shift-plus-axis-drag duplication and the explanatory block from the movement-distance editor; duplication remains available only through the explicit properties-panel button. In its place, a single selected profile now presents persistent JLCFA-inspired assembly verification: profile length, target-end clearances at real connections, the nearest unobstructed X/Y/Z gap to another profile, and an amber highlight on every actual non-interfering contact region. Dimensions and connection confirmation come from current scene geometry, update during movement/camera orbit, and disappear when contact is broken rather than depending on a short-lived snap event.
- **2026-08-11:** Replaced free part dragging and right-drag camera orbit with a deterministic mouse model aligned to the clarified MayCAD/JLCFA reference: left-button dragging always orbits the camera regardless of selection; a short left click still selects; moving requires one selected unlocked item and a right-drag on one of six signed world-axis arrows (±X/±Y/±Z). Part bodies cannot be freely dragged, the former XY/XZ/YZ work-plane and Shift-marquee interactions are removed, and right-click without dragging retains rotate/delete. Profile length handles also use right-drag.
- **2026-08-10:** Refined compact profile BOM grouping from literal A/B/C/D labels to proven manufacturing equivalence under rotation around the profile's own length axis. The complete profile must overlap after one permitted roll, including every hole face, opposite exit, physical groove, position, hole/thread/fastener type, end tapping, and miter state. Initially only verified ordinary square open profiles `1515`, `2020`, `3030`, and `4040` permit 90° increments; this makes ordinary 2020 A→C and C→A through-hole instructions equivalent. Covered/rounded/asymmetric sections, multi-slot faces, rectangular profiles including 2040, multi-port tapping, and mitered profiles remain orientation-sensitive until an explicit symmetry and slot/port mapping is verified. When equivalence cannot be proven, rows must not be merged.
- **2026-08-10:** Corrected the Chinese product name from “宜家书法特柜子” to “宜家舒法特柜子” across storefront, configurator, generated production remarks, catalog documentation, and admin order labels. Stable internal identifiers such as `calligraphy_cabinet` remain unchanged, and backend classification continues recognizing the former Chinese spelling so historical orders remain compatible.
- **2026-08-10:** Separated fitted 3D screw shaft length from the order SKU. The former 27/28mm values were calculated geometry lengths and are retained only as `renderedScrewLengthMm`; 20-series cart/PDF rows now use the confirmed catalog specifications cylinder-head M6×30 and button-head M6×20 (flat-head M6×8 where explicitly requested). The PDF screw-type cell includes the full thread×length specification.
- **2026-08-10:** Parameterized furniture entry points open the generated 3D designer in a new browser tab/window. The calligraphy-cabinet or wardrobe product configuration page must remain open with the customer's entered dimensions, because the full-screen designer has no in-app route back to that product configurator. The same-origin template handoff remains local and one-time; it is consumed by the new designer tab without navigating the source page.
- **2026-08-10:** Corrected No.9 three-way connector placement semantics. It must attach only to a true three-profile node: three compatible profiles with mutually perpendicular length axes and endpoints converging at one corner. Its position is derived from all three end faces (with a common-endpoint fallback for legacy scenes), its local +X/-Y/+Z ports are oriented toward the three profiles, and the locked attachment records all three profile IDs. No.9 must never reuse an arbitrary two-profile joint candidate; previously saved two-profile No.9 locks are migrated to the nearest valid three-way node when possible or unlocked for review.
- **2026-08-10:** Extended compact BOM grouping to profiles. The designer project summary and customer/factory PDF may show one row for multiple profiles only when the complete physical manufacturing signature matches: product/model, length, finish/color, unit price and label service, every hole's face/position/type/thread/physical groove/fastener override, every tapping port, and both miter ends. Scene position, rotation, IDs, and generated assembly-location remarks do not split an otherwise identical manufacturing row; distinct location remarks are retained in the derived display data, while the original editable scene/cart and production JSON/XLSX remain per-profile and unchanged. Any physical machining difference must create a separate row.
- **2026-08-10:** Unified identical-accessory BOM grouping across the designer project tree and customer/factory PDF. Profiles and boards remain individually listed when their manufacturing or dimensional identity matters; shelf supports, screws, connectors, feet, and future accessory kinds are compacted by complete purchasing specification (kind/SKU, compatible profile series, dimensions or screw head/length, color, and unit price). Scene entities and their transforms/links remain separate and editable internally. Grouped cart/PDF rows retain all positions, linked-hole references, attachment references, and distinct manufacturing remarks in structured arrays, while the visible BOM shows one row with the summed quantity. Differing location text alone must not split otherwise identical accessory rows.
- **2026-08-10:** Shortened the calligraphy-cabinet top clearance without removing a basket level. Upright holes remain 130mm, 260mm, 390mm, and so on from the bottom end, but the final hole is only 65mm from the upright top. Generated height is therefore `layers×130 + 65 + 40` mm (755mm for five layers), while the structure still contains bottom frame + `N` basket-support levels + top frame. This supersedes the provisional 820mm five-layer height, whose 130mm top clearance wasted 65mm.
- **2026-08-10:** Superseded the preceding calligraphy top-depth assumption after physical-structure clarification. A cabinet with `N` basket layers has `N+2` distinct depth levels: one bottom frame level without shelf supports, `N` intermediate basket-support levels with shelf supports, and one top frame level without shelf supports, directly under the marine board and coplanar with the top continuous rails. Therefore a five-layer cabinet has seven depth levels, and generated height is `(layers+1)×130 + 40` mm (820mm for five layers); the previous 690mm height only left room for four intermediate basket levels. Top and bottom frame depth rails are both retained and must not be treated as basket supports.
- **2026-08-10:** Removed the calligraphy-cabinet top-rail interference. On each divider line, the highest 420mm basket-support profile also serves as the one and only top depth tie; the template must not generate a second nearly overlapping top depth profile. Separate 420mm bottom depth ties remain. Because the top continuous front/rear rails no longer terminate an additional depth profile, their obsolete side-face countersunk holes and linked screws are also omitted; their through holes for the uprights remain.
- **2026-08-10:** Corrected the calligraphy-cabinet vertical machining datum: the first shelf/basket suspension hole is 130mm from the bottom end of each upright, never half-pitch at 65mm. Subsequent holes and their aligned depth support rails are 260mm, 390mm, 520mm, and so on in exact 130mm increments, so the lowest 100mm basket clears the bottom frame.
- **2026-08-10:** Refined furniture fastener semantics. Every aluminum profile generated by the calligraphy-cabinet and wardrobe templates now defaults to both-end tapping, and every eligible through/countersunk hole is generated with a linked screw already present. Calligraphy shelf supports are slim slot-in ledges embedded 2mm into the 2020 groove and protruding 10mm, rendered as a simplified strip. The top/bottom continuous calligraphy rails carry separate through-hole records for the vertical-post connections in addition to the depth-tie holes.
- **2026-08-10:** Replaced the provisional wardrobe B/D-post-hole arrangement with the confirmed “horizontal caps vertical” joint. Each 2040 post is self-rotated so the 20mm face is outward and the 40mm face runs along the cabinet side. At the top and bottom, two perpendicular 2020 rails occupy the two 20mm halves of the 2040 end face; each 2020 carries its own countersunk connection to one 2040 tapping port. The continuous front/rear 2020 rails carry the through holes for separate flat-head M6×8 screws that enter the tapped ends of the perpendicular depth rails. `flat_socket` is a first-class screw head in designer/cart/PDF/XLSX output and uses the same unit-price rule as the existing 2020 half-round/button-head screw.
- **2026-08-13:** Corrected the wardrobe M6×8 side connection orientation and 3D seating. The B/D through-hole face at each joint is explicitly marked as the screw's exit side: the flat-head screw is rotated 180° relative to an ordinary face-entry screw and travels outward from that marked face into the tapped end of the perpendicular depth rail. This exceptional direction survives design JSON and production-XLSX round trips instead of being inferred from the wardrobe name. Flat/button heads seat on the joint-side surface, and the renderer preserves the true 8mm shaft length instead of stretching it to 16mm; this keeps the short side screw clear of the perpendicular countersunk bore.
- **2026-08-13:** Corrected screw visibility in every designer assembly, including generated calligraphy-basket cabinets. With opaque profiles, a linked counterbore/countersunk connection renders only its realistic silver seated socket head and the top lip exposed at the groove; the threaded shaft, spring fastener, and the former amber/black machining helper must not appear through or over the aluminum. Empty machining holes still retain their process markers. Enabling profile translucency (including the translucent accessory-inspection state) restores the complete screw shaft and internal fastener/tapped-profile relationship for assembly review. This is a display-only rule and does not alter screw BOM, machining, or saved scene data.
- **2026-08-13:** Applied the same physical-visibility split to linked through-hole connections. With opaque profiles, only the single plain circular opening on the camera-visible profile wall is shown; the opposite-wall opening remains normally occluded. This is a wrench/access hole: its button head seats internally at the slot beside the mating profile, and the head, complete catalog-length shaft, receiving relationship, and shallow rectangular 3030 elastic T-slot fastener become visible only with profile translucency. Counterbore/countersunk connections continue using the separate exposed-silver-head rule above, and empty holes continue showing machining marks.
- **2026-08-13:** Restored No.9 three-way installation hotspots at valid three-axis corner nodes. The detector already calculated candidates from three mutually perpendicular same-series profiles and their three converging end faces, but mistakenly returned the separate two-profile candidate collection, which is intentionally empty for No.9. No.9 now returns the computed three-profile candidates, keeps all three attachment IDs, and continues applying any required half-module end shortening before installation.
- **2026-08-13:** Confirmed No.9 physical geometry from the supplied end-connector reference. It is always an exact profile-series cube—1515 is 15×15×15mm, 2020 is 20×20×20mm, and 3030 is 30×30×30mm. Its three local mating faces receive the three mutually perpendicular profile ends flush; circular installation openings are rendered on the opposite three exposed faces. Saved/imported No.9 dimensions are normalized back to this catalog cube so the former 30×30×20 default cannot persist.
- **2026-08-13:** Reduced false No.7 L-plate hotspots at crowded corners. After establishing a coplanar exterior face on the two intended profiles, the candidate now tests the complete L-plate footprint against every non-target profile. At a three-axis node, the face directed into the third profile is rejected because the plate would be blocked, while the opposite clear face remains available. Minimum arm clearance and two-profile surface contact checks still apply.
- **2026-08-13:** Removed meaningless single-profile No.1 corner-bracket placement inherited from the MayCAD reference. The reference defines the physical No.1 model and its possible groove orientations, but customer placement requires a real joint: two different, mutually perpendicular, physically contacting profiles. Each of the bracket's two straight 90-degree seating edges must attach to one target profile, so every valid placement records exactly two profile IDs. The renderer evaluates both exterior-face orientations, automatically flips the bracket to follow the available side, and rejects any flipped body that would enter a third profile. Historical one-profile No.1 locks are no longer treated as valid manufactured connections.
- **2026-08-13:** Flattened the first part-library choices. The aluminum-profile model selector and Free Draw action lead the library, followed by cabinet door, aluminum plate, pegboard, and marine board without separate “profiles” and “panels” category headings. “Restore cursor” is not a part-library item: it returns to the 3D canvas upper-left as a contextual purple control while free draw, pointer-based accessory placement, or point-drilling is active, and Esc performs the same recovery. The control disappears in ordinary selection/movement mode so the canvas keeps one stable, unambiguous recovery location without occupying library space.
- **2026-08-13:** Tightened accessory placement to manufacturing contact geometry. No.7 T/L plates may be generated only on an exterior surface plane shared by both target profiles; the complete crossbar/stem or both L arms must sit on the profiles rather than hover across a center-plane mismatch. Mixed sections such as 2020 against 2040 therefore use each profile's real projected exterior face instead of the larger half-section as a shared offset. No.1 corner brackets use the true shared inside corner of two perpendicular profiles: both die-cast seating faces touch their respective extrusion, and neither full-width arm may enter either target or a blocking third profile.
- **2026-08-14:** Corrected the No.7 L-plate origin and support test from the supplied physical 3030L reference. The plate exists only at a real 90-degree contact between two profiles on one shared exterior plane. Its local origin is the centre of that contact; one complete arm is centred across and supported by the first profile, while the other complete arm is centred across and supported by the perpendicular profile. Each arm fills the matched profile module, all four seating corners of both arms must remain on their intended extrusion, and third-profile blocking is tested against the actual L-shaped arms rather than the empty area of an enclosing square. Four fasteners remain on the two fully seated arms, two per profile.
- **2026-08-14:** Applied the same complete-support requirement to the No.7 T plate from the supplied 3030T reference. It installs only where two profiles physically meet at 90 degrees on one coplanar exterior face. Because the detected solid contact lies on the perpendicular member's butt face, the visual plate origin steps back half a profile module to the continuous member's centreline: the full crossbar is centred there in both directions and the full stem starts there. The stem's first half-module is supported by the continuous profile before support transfers to the perpendicular profile. Both pieces fill the matched profile module; every seating corner must lie on the union of those two real aluminum surfaces, and each of the four fasteners remains over a profile. Any unsupported overhang or obstruction by a third profile removes that candidate instead of rendering an unrealistic plate.
- **2026-08-14:** Matched the supplied JLCFA free-draw corner assistance. Each profile end now offers one end-face-centre magnetic start plus four distinct end-edge side starts. The centre state produces a non-interfering perpendicular profile across the selected end face; a side state locks the new profile's drawing axis to that side normal, butts its end against the side wall, and moves its cross-section inward by its own half-module along the target length so the two outside end faces are flush. Existing and immediately preceding profiles preferentially offer non-interfering 90-degree world-axis continuations. This replaces the former single 22px endpoint-centre target that stole side gestures and made top/aside frame construction unreliable.
- **2026-08-14:** Replaced the unreadable bottom-right scene instruction overlay. The long translucent sentence is now an opaque dark, high-contrast card: left-drag orbit, right-drag pan, and wheel zoom are separate compact chips, while selected-part axis movement and short-right-click actions occupy a second line. The card stays at the canvas edge and no longer loses its text against the pale grid or translucent profiles.
- **2026-08-10:** Revised the calligraphy-cabinet depth structure after visual review. At each 130mm basket level, every divider line now has one centered 420mm depth profile aligned directly with the front/rear upright hole centers; adjacent baskets share that rail. Interior rails receive shelf-support placeholders on both sides, while the two outer rails receive only inward-facing supports, preserving exactly two supports per basket without duplicated profiles. Every divider also receives a 420mm depth tie at the top and bottom, and the continuous front/rear top and bottom rails receive matching countersunk hole records.
- **2026-08-10:** Added parameterized furniture templates that enter the normal editable designer rather than maintaining a separate renderer. The IKEA calligraphy-basket cabinet uses 300×420×100mm basket bays, 130mm vertical pitch, fixed 460mm outside depth, all-2020 frame members, continuous full-width front/rear top and bottom rails, shared 420mm depth rails on each divider, exactly two shelf supports per basket, and one continuous 12mm marine-board top. Overall generated length is `columns×300 + (columns+1)×20`; overall height is determined by the current cabinet-dimension formula. Length/height bound mode treats both inputs strictly as maximum occupied-space limits and chooses the most columns and layers whose complete grid does not exceed either limit; it does not promise an exact made-to-input outside size. The former 2440mm/seven-column cap is superseded by the 2026-08-12 overall-limit rule below.
- **2026-08-11:** Confirmed shelf-support pricing by actual cut length: oxidized natural is ¥8/m, colored with a natural section is ¥10/m, and colored with a colored section is ¥12/m. The selected tier and calculated price must survive design JSON, cart/PDF grouping, production JSON, and Excel. Updated the calligraphy-cabinet bound-mode wording in all three languages to state explicitly that length and height are maximum space limits used to maximize complete basket columns/layers; actual generated dimensions may be smaller and are not an exact-fit promise.
- **2026-08-11:** The designer screw property card and project list now display the same purchasing specification returned by `getDiyScrewOrderSpec` as cart/PDF output, keyed by compatible profile series and screw head. Fitted values such as 27/28mm remain internal 3D rendering geometry only and are no longer editable or presented as the order length. For example, 2020 cylinder-head screws display M6×30 and 2020 button-head screws display M6×20; other series resolve through the same shared specification function.
- **2026-08-11:** Replaced the supplied profile-color photography with centered, margin-preserving 900×1200 high-detail JPEG assets that prioritize visible matte/gloss/metallic texture. Updated customer-facing names to the current physical sample labels, added 可乐红 and 墨青绿 as colored-cut-end-only options, kept colors without a supplied sample unchanged, and added the rotated all-color overview to the compact cut-end guide. The overview is lazy-loaded and disappears after a customer chooses a specific color; individual swatches are likewise loaded only when selected.
- **2026-08-11:** Superseded the long-term 3D screw-rendering rule: internal screw geometry should ultimately use the same catalog length as the purchasing/PDF specification. Legacy fitted 27/28mm scene geometry remains a known deferred mismatch for now; the customer/factory specification is already catalog-correct and continues to take precedence.
- **2026-08-20:** Extended the wardrobe template from one bay to a customer-selected `N` columns. Columns divide the entered front length evenly and create `N+1` front/rear 2040 post boundaries. The top and bottom retain continuous front/rear 2020 rails, with one depth rail at every boundary; the fixed-frame count is therefore `4N+8` and reduces to the original 12 members when `N=1`. At every storage level, front/rear 2020 rails are split into one segment per column so no rail passes through an intermediate 2040 post, while adjacent columns share the depth rail on their common boundary. Each column is consequently a closed four-sided frame; a level contains `3N+1` profiles and `6N+2` No.1 bracket connections. Total profile count is `(4N+8) + storageLayers×(3N+1)`. Front and rear shelf segments retain the inward-groove rule on the 2040 wide face, and every bracket keeps both perpendicular seating edges flush to its rail and post. Storage-layer rails remain untapped and posts receive no storage-height countersunk holes. Levels remain evenly distributed between the top and bottom frames, the outer frames are not counted as storage levels, the minimum vertical clearance remains 80mm, and every wardrobe profile remains capped at 3000mm. The UI limits columns to nine and also enforces at least 80mm between adjacent post centerlines so bracket bodies cannot overlap.
- **2026-08-20:** Added a physical shelf surface to every generated wardrobe storage frame. Every storage level in every column receives one inset 12mm UV marine-board shelf, so the board count is `storageLayers×columns`. Board length and depth are each 2mm smaller than the corresponding clear opening of the surrounding 2020 frame, leaving a 1mm clearance on all four sides; its top face sits flush with the top of the 2020 rails. Every board receives exactly eight point supports, two per side. These are a distinct catalog/scene item named `12mm板专用层板托`, not the existing continuous shelf-support strip that is priced by the meter. The dedicated support is fixed at ¥0.7 per piece, carries the `board_12mm` shelf-support subtype through design/production/cart data, and totals `storageLayers×columns×8` pieces.
- **2026-08-21:** Extended the same inset 12mm UV wardrobe shelf to every column of the bottom and top outer frames. A wardrobe now contains `(storageLayers+2)×columns` boards and `(storageLayers+2)×columns×8` dedicated `12mm板专用层板托`; top and bottom boards follow the same 1mm-per-side clearance, flush top-face, attachment, pricing, cart, and production-export rules as intermediate storage boards. In the normal opaque assembly view, connector-hole and socket decorations obey depth occlusion so hardware beneath a board cannot appear as dirt-like dots on its surface. Translucent profile inspection may still reveal those internal details.
- **2026-08-20:** VIP+ accessory freight uses zero billable accessory weight regardless of accessory quantity or order amount, but only when a signed-in user's explicit account status normalizes to `vip_plus`. Accessories therefore never add shipping cost for an authenticated VIP+ customer; profiles, boards, and other freight-bearing goods in the same cart continue contributing their normal weight. Standard, VIP, and unregistered/guest orders containing accessories retain the existing rule that an entire cart subtotal below ¥30 contributes one unified 1kg minimum accessory shipping weight and reaches zero accessory shipping weight when the combined value of all products in the cart reaches ¥30.
- **2026-08-20:** Quick Quote presents province, product type, and shipping method as three peer dropdowns in one row on desktop. Shipping rate calculations and tier rules remain unchanged, but the shipping control no longer expands first-weight, additional-weight, or overlength details; each courier option may show only its currently estimated fee.
- **2026-08-20:** Auto-fit cabinet doors are column-aware. An N-column front frame produces N independent door leaves rather than one leaf spanning divider profiles. Full-overlay leaves cover the frame and meet at each divider with a 2mm gap. Large-bend/inset leaves remain entirely inside each column's clear opening, with an additional 2mm gap on all four sides. Every leaf is priced, exported, and kept attached to its own pair of adjacent posts plus the common top/bottom rails.
- **2026-08-20:** Added accessory inventory management parallel to profile inventory. The backend tracks finished accessory quantities by exact catalog SKU, compatible profile size, and finished color. Administrators have a dedicated accessory-inventory tab with accessory/size/color filters, direct piece-count editing, and `.xlsx` export/import; the public endpoint exposes only the current piece counts and catalog labels. The customer accessory quote table displays the matching live quantity for the selected profile size and exact natural/colored finish. As with the existing profile-stock display, zero stock is informational for now and does not silently disable ordering until a separate back-order policy is confirmed.
- **2026-08-12:** The wardrobe template originally kept a 12-profile outside frame from entered L/W/H and added customer-selected internal storage levels. Each level was one complete four-member 2020 rectangle with eight No.1 bracket contacts, for `12 + storageLayers×4` profiles. This single-column count and geometry are superseded by the 2026-08-20 N-column rule above; at `N=1` the new generator remains geometrically and numerically backward-compatible.
- **2026-08-12:** The calligraphy-basket cabinet is capped at 3000mm overall generated length and 1600mm overall generated height. Grid mode therefore permits at most nine 300mm bays and eleven 130mm-pitch basket layers under the current frame formula; bound mode continues treating the entered values as occupied-space maxima rather than exact finished dimensions. This supersedes the earlier 2440mm overall-product cap without exceeding marine-board stock: the 12mm top remains one piece through 2440mm and automatically becomes two pieces above that length, with the seam centered on an existing 2020 divider so both board ends remain supported and neither piece exceeds 2440mm.
- **2026-08-12:** Matched the supplied JLCFA movement reference more closely. Axis dragging is bidirectional and displays a compact pointer-following signed distance to the nearest viable connection (`+` on approach, `0.0mm` at snap, `-` after crossing), then restores the displaced exact-value editor on release. Profile snap acquisition is narrowed to 0.72× the larger cross-section and release to 0.34×, replacing the earlier provisional 2× cap; the large cyan ghost/ring and verbose snap banner are removed. The selected profile remains blue during valid motion and turns red immediately during live solid interference. Accessory snapping retains its existing range and feedback.
- **2026-08-12:** Refined blue-axis dragging against the supplied 21:48 JLCFA recording. Added axis-only outside-face and end-face flush candidates, paired amber face patches, a 5mm drag grid, and a true magnetic detent. Joint acquisition remains 0.72× the section; flush acquisition is 0.28× and release resistance is 0.55×. Pulling through a detent now consumes that pointer travel and resumes from the exact snapped plane without a visual jump. The live signed label remains the gesture's total displacement and is positioned beside the moving gizmo/profile, matching the reference's meaning and placement more closely.
- **2026-08-12:** Made long-profile editing survive close inspection. The translation gizmo now uses a separate viewport-aware anchor on the selected profile instead of remaining at its geometric midpoint; zooming/panning to an end keeps the gizmo near the visible profile segment, and an active drag freezes that offset while moving the entire item. Raised side-hole openings, countersunk amber rims, threaded markers, and linked screw meshes above the profile depth/highlight layer so subtractive machining and seated fasteners remain visible instead of being occluded by the extrusion.
- **2026-08-12:** Enabled the supplied threaded-stem caster, adjustable-foot, and aluminum end-cap references in the designer's other-hardware library. The caster is fixed black with M6/M8/M10/M12 and brake/no-brake options; the adjustable foot is fixed gold, 70×70mm with a 28mm post, M8 adjustment point, and 60-68mm height. End caps bind to exact profile ends, support every color, auto-add missing tapping, and remove only tapping they own. JSON/XLSX/cart specifications preserve these selections and relationships. Clearly labelled provisional estimates are caster ¥18 base (+¥4 brake, +¥2 M10, +¥4 M12), foot ¥12, and end cap ¥6 natural/¥8 colored.
- **2026-08-10:** The initial wardrobe template produced exactly 12 profiles from entered outside L/W/H: eight horizontal 2020 rails and four full-height 2040 posts. Horizontal rails received both-end tapping; each 2040 post received top/bottom countersunk holes on adjacent B/D faces. This fixed-frame-only rule is superseded by the 2026-08-12 storage-layer rule above.
- **2026-08-10:** Added a repository-local pre-push release mechanism. After the one-time `npm run setup:git-hooks`, every push rebuilds the frontend and atomically replaces `~/Downloads/mengkaile-dist-latest.zip`. Build or packaging failure blocks the push, ensuring the upload ZIP corresponds to the source about to be pushed.
- **2026-08-09:** Added a lightweight, multilingual startup progress screen in the critical HTML. It remains below 100% until React paints the usable application and then fades out, so slow connections receive immediate loading feedback.
- **2026-08-09:** Removed automatic 3D position/rotation text from printable remarks. Production-sheet previews with no address or zero shipping fee now display freight collect rather than ordinary prepaid courier service.
- **2026-08-08:** Corrected MayCAD `PROF20-2040` to Mengkaile ordinary `2040`. `2040-N1-40` in the chair reference is a post-import manual replacement, not an automatic source mapping. Added a maintained verified-intersection table; unknown MayCAD profiles use dimension-only fallback with an explicit review warning.
- **2026-08-09:** Expanded the deterministic MayCAD intersection map from 4 to 37 exact source codes using `maycad_mengkaile_aluminum_profile_common.scene`. Corrected 20×20 `21SP` to `2020R`, added verified cover variants, and mapped 30×30 Soft/two-face/three-face/four-face groups to `3030R`/`3030-N2`/`3030-N1`/`3030`; approved 3060/4040/4080 variants normalize to their ordinary Mengkaile models.
- **2026-08-09:** Standardized the Chinese brand name as “萌开了”. Added mandatory post-import model selection for unknown, unverified, and PDF-inferred MayCAD profiles. Clarified in the UI that the current importer supports profiles and recognized drilling only; accessory import remains under development.
- **2026-08-09:** Restricted the customer-facing MayCAD import picker to `.scene` files. Non-`.scene` selections are rejected before parsing with localized guidance; PDF reconstruction is no longer exposed through this picker.
- **2026-08-09:** Disabled all MayCAD AI execution by default. Removed the frontend PDF API method and made backend AI route registration opt-in through `ENABLE_MAYCAD_AI_IMPORT=1`; normal local and cloud deployments expose only deterministic `.scene` import and require no AI API key.
- **2026-08-08:** Unified legacy “配304螺丝” ordering with explicit 3D-designer screws. Explicit designer screw quantities now override the legacy per-hole add-on, always trigger the factory reminder, and export as compact specification-grouped PDF rows with no duplicate per-screw cards.
- **2026-08-08:** Reversed collision handling from blocking/reverting transforms to MayCAD-style non-blocking interference feedback. Movement and rotation now remain applied; involved profiles and project nodes are highlighted red until resolved. Added scene-level rotation controls and an explicit right-click rotate/delete hint.
- **2026-08-01:** Consolidated the initial 3D designer implementation and all subsequent product-review decisions into this durable project document.
